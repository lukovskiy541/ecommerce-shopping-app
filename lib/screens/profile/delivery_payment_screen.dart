import 'package:flutter/material.dart';

class DeliveryPaymentScreen extends StatelessWidget {
  const DeliveryPaymentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Delivery & Payment'),
      ),
      body: Center(
        child: Text('Delivery & Payment Screen'),
      ),
    );
  }
}